@page "/regression"

<PageTitle>Simple Regression</PageTitle>

<h3>Simple Regression</h3>
<p>Wypełnij dane o artykule i zobaczysz jego przewidywaną liczbę wyświetleń.</p>

<div class="mb-3">
    <label for="titleLengthInput" class="form-label">Długość:</label>
    <input type="number" id="titleLengthInput" class="form-control" @bind="_titleLength"></input>
</div>

<div class="mb-3">
    <label for="keywordsCountInput" class="form-label">Ilość słów kluczowych:</label>
    <input type="number" id="keywordsCountInput" class="form-control" @bind="_keywordsCount"></input>
</div>

<div class="mb-3">
    <label for="categoryInput" class="form-label">Kategoria:</label>
    <input id="categoryInput" class="form-control" @bind="_category"></input>
</div>

<div class="mb-3">
    <label for="authorExperienceInput" class="form-label">Doświadczenie autora:</label>
    <input id="authorExperienceInput" class="form-control" @bind="_authorExperience"></input>
</div>

<button class="btn btn-primary" @onclick="Start">Start</button>
<button class="btn btn-primary" @onclick="StartWithModelFromFile">StartWithModelFromFile</button>

@if (!string.IsNullOrWhiteSpace(_result))
{
    <div class="mt-3">Wynik: @_result</div>

    <div class="mt-3">@((MarkupString)_metrics)</div>
}